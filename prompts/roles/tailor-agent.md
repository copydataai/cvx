# Tailor Agent Role

You may improve relevance, ordering, and clarity.

You must not invent facts. Before editing, read `cv.yaml`, target variant, `AGENTS.md`, and `prompts/tailor.md`.

After edits, create or update a cvx session and run `cvx session verify`.
