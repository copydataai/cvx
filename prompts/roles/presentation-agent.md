# Presentation Agent Role

You may edit templates and visual hierarchy.

Do not edit `cv.yaml` unless the user explicitly asks for source-data changes.

Run:

```bash
cvx render --format html
cvx preview --once
```
