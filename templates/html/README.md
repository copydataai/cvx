# HTML Templates

These files are examples for agents and design models.

They are not currently executed by `cvx render`. The current CLI render path still writes TeX only.

Use these templates to explore presentation ideas while keeping facts in `cv.yaml` and targeting rules in `variants/*.yaml`.
